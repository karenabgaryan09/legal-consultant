import { Button } from "@/components";

export const metadata = {
    title: "Corporate",
    description: "this is Corporate page description",
};

const Corporate = () => {
    return (
        <main>
            <div className="container">
                <h1>Corporate sub page</h1>
            </div>
        </main>
    );
};

export default Corporate;
