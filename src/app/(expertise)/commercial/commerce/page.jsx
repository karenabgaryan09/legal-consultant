import { Button } from "@/components";

export const metadata = {
    title: "Commerce",
    description: "this is Commerce page description",
};

const Commerce = () => {
    return (
        <main>
            <div className="container">
                <h1>Commerce sub page</h1>
            </div>
        </main>
    );
};

export default Commerce;
