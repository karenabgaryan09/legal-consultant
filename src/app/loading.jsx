import { Header } from "@/components";

const Loading = () => {
    return (
        <>
            <Header />
            <main>
                <section>
                    <div className="container">loading...</div>
                </section>
            </main>
        </>
    );
};

export default Loading;
